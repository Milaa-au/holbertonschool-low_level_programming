#include <stdio.h>
#include "main.h"

/**
 * mul - Multiplie deux entiers entre eux
 * @a: est le premier entier
 * @b: est le deuxieme entier
 *
 * Return: les entiers multiplies
 */

int mul(int a, int b)
{
	return (a * b);
}
